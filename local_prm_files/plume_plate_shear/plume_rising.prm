#include ../local_prm_files/plume_plate_shear/track_cpo.prm

set Dimension                              = 2
set End time                               = 2e8
set Use years instead of seconds           = true
set Output directory                       = $ASPECT_SOURCE_DIR/../output/plume_plate_shear/plume_rising_cpo

set Maximum first time step                = 1e5
set Maximum time step                      = 1e8
#set Maximum relative increase in time step = 
set Nonlinear solver scheme                = iterated Advection and Stokes
set Nonlinear solver tolerance             = 1e-5
set Nonlinear solver failure strategy =  cut timestep size
#set CFL number                             = 0.5
#set Use operator splitting = true

subsection Solver parameters
  subsection Stokes solver parameters
    set Stokes solver type = block GMG
    set Number of cheap Stokes solver steps = 6000
    set Linear solver tolerance = 1e-8
    set GMRES solver restart length =100
  end
end

# subsection Discretization
  
#   set Use discontinuous composition discretization = true
#   subsection Stabilization parameters
#     set Use limiter for discontinuous composition solution = true
#     set Global composition maximum = 1.0
#     set Global composition minimum = 0.0
#   end
# end


subsection Geometry model
  set Model name = box

  subsection Box
    set X extent = 1200.e3
    set Y extent = 600.e3
    set X repetitions = 6
    set Y repetitions = 3
  end
end

# subsection Mesh refinement
#   set Initial adaptive refinement        = 0
#   set Additional refinement times        = 3.5e7
#   set Initial global refinement          = 5 # corresponds to 3x3km
#   set Minimum refinement level           = 3 # corresponds to 25x25km
#   set Coarsening fraction                = 0.001
#   set Refinement fraction                = 0.05
#   set Strategy                           = viscosity
#   set Time steps between mesh refinement = 5 # 0 # no mesh refinement 
# end

subsection Mesh refinement
  set Initial adaptive refinement        = 1
  set Initial global refinement          = 5 # corresponds to 3x3km
  set Minimum refinement level           = 3 # corresponds to 25x25km
  set Coarsening fraction                = 0.2
  set Refinement fraction                = 0.6
  set Strategy                           = viscosity,  temperature, boundary
  set Time steps between mesh refinement = 1 # 0 # no mesh refinement 
  subsection Boundary
    set Boundary refinement indicators = top
  end
end

# # # Boundary Conditions and Drivers # # #

subsection Gravity model
  set Model name = vertical
  subsection Vertical
    set Magnitude = 9.81
  end
end

subsection Boundary velocity model
  set Tangential velocity boundary indicators = left, right, bottom
  set Zero velocity boundary indicators = top
  # set Prescribed velocity boundary indicators = left, right, top, bottom
  # subsection Function
  #   set Variable names = x, z
  #   set Function constants =  v =     0.015,   / # m/yr
  #                             DLT =   150.e3, / # depth of lithosphere-asthenosphere boundary
  #                             Dbox =  600.e3, / # box depth
  #                             DAS =   150.e3  / # asthenosphere thickness
  #   set Function expression = z>= Dbox-DLT-DAS ? (z>=Dbox-DLT ? v : v*(z - (Dbox-DAS-DLT))/DAS ) : 0 ; 0; 
  # end
end

subsection Boundary temperature model
  set Fixed temperature boundary indicators = bottom, top
  set List of model names = function

  subsection Function
    set Variable names      = x, z, t
    set Function constants = x0=600.e3, sigma=300.e3, plume_end=1e9,\
                             Dbox=600.e3, DLAB=150.e3, \
                             dTP=250, TLAB=1500, TLT=273, TLM=1550 # K/m
    set Function expression = z>= Dbox - DLAB \
                              ? TLAB - (TLAB-TLT)*(z-Dbox+DLAB)/DLAB \
                              : (t< plume_end \
                                ? TLM + dTP*exp( -0.5*( (x-x0)/sigma)^2) \
                                : TLM )
  

    # set Function expression = z>= Dbox - DLAB \
    #                           ? TLAB - (TLAB-TLT)*(z-Dbox+DLAB)/DLAB \
    #                           : (z!=0 ? TLAB + (TLM-TLAB)/(Dbox-DLAB)*(Dbox-DLAB-z) : (t< plume_end ? TLM + dTP*exp( -0.5*( (x-x0)/sigma)^2) : TLM ) )
  end
# plume_end=1e7, t<plume_end # TLAB - (TLAB-TLT)*(z-Dbox+DLAB)/DLAB # TLAB + dTdz*(Dbox-DLAB-z)
end

subsection Initial temperature model
  set Model name = function

  subsection Function
    set Variable names      = x,z
    set Function constants = Dbox=600.e3, DLAB=150.e3, \
                             TLAB=1500, TLT=273, TLM=1550 # K
    # linear temperature gradient above and below LAB
    set Function expression = z>= Dbox - DLAB \
                              ? TLAB - (TLAB-TLT)*(z-Dbox+DLAB)/DLAB \
                              : TLAB + (TLM-TLAB)/(Dbox-DLAB)*(Dbox-DLAB-z)
  end 
end

# # # Composition and material model # # # 

# subsection Formulation
#   set Formulation = Boussinesq approximation 
# end

subsection Material model 
  set Model name = depth dependent

  subsection Depth dependent model
    set Base model = visco plastic
    set Depth dependence method = List
    set Depth list = 300e3, 600e3
    set Viscosity list = 5e21, 1e24
    set Reference viscosity = 1e22
  end
  
  subsection Visco Plastic
    set Reference temperature                       = 273
    set Minimum viscosity                           = 1e19
    set Maximum viscosity                           = 1e29
    set Thermal diffusivities                       = 0.8e-6
    set Heat capacities                             = 1250
    #set Thermal diffusivity                         = 0.8e-6
    #set Heat capacity                               = 1250
    set Densities                                   = 3300
    set Thermal expansivities                       = 3.5e-5
    set Viscosity averaging scheme                  = harmonic

    set Viscous flow law                            = composite
    # diffusion creep 
    set Prefactors for diffusion creep              = 8e-12
    set Stress exponents for diffusion creep        = 1
    set Activation energies for diffusion creep     = 250e3
    set Activation volumes for diffusion creep      = 0 # 6e-6
    
    set Grain size = 1e-3
    set Grain size exponents for diffusion creep    = 0
    
    # dislocation creep 
    set Prefactors for dislocation creep = 8e-12 # 1.1e-16
    set Stress exponents for dislocation creep = 1 # 3.5
    set Activation energies for dislocation creep = 250e3 # 5.3e5
    set Activation volumes for dislocation creep = 0 # 1.4e-5

    # Plasticity parameters - irrelevant
    # set to very high value so that it is not used
    set Cohesions = 1e20
  end 
end


subsection Postprocess
  set List of postprocessors = visualization, velocity statistics, temperature statistics

  subsection Visualization
    set Time between graphical output = 2.e7
    set List of output variables = material properties # , named additional outputs
  end
end

include $ASPECT_SOURCE_DIR/local_prm_files/plume_plate_shear/track_cpo.prm
