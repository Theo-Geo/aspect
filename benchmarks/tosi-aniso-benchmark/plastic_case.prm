include $ASPECT_SOURCE_DIR/benchmarks/tosi-aniso-benchmark/Tosi_base_CPO.prm

set Additional shared libraries            = $ASPECT_SOURCE_DIR/benchmarks/tosi-aniso-benchmark/plugin/libtosi-aniso.so
set Output directory                       = output-tosi-plastic-isotropic

# setting the fields to static stops the mapping from particles to grid
# this essentially disables the coupling 
# to reduce runtime one could not advect particles all together 
subsection Compositional fields
  set Number of fields = 14
  set Compositional field methods = prescribed field, \
                                    static, static, static, static, \
                                    static, static, static, static, \
                                    static, static, static, static, \
                                    static
  set Mapped particle properties  = 
end

subsection Material model
  subsection Tosi benchmark
  
    set Rate factor         = 1   # default
    set Power law exponent  = 1e5 # default, represents n \to \infty
    set Base model          = CPO-induced anisotropic viscosity
  end 

  subsection CPO-induced Anisotropic Viscosity 
    set Stress exponent     = 1e5
  end

end

# reduce number of particles when interpolation is not needed
subsection Particles
  set Minimum particles per cell  = 1
  set Maximum particles per cell  = 2
end 